

import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF171616);       // Dark background color
  static const Color accentColor = Color(0xFFE7900F);        // Orange accent color for buttons
  static const Color textColor = Colors.white;               // Main text color
  static const Color textSecondaryColor = Colors.grey;       // Secondary text color
  static const Color buttonTextColor = Colors.white;         // Text on buttons
  static const Color cardColor = Color(0xFF333333);          // Darker shade for cards and containers
  static const Color successColor = Color(0xFF4CAF50);       // Success color (e.g., for positive feedback)
  static const Color errorColor = Color(0xFFF44336);         // Error color (e.g., for negative feedback)
}
